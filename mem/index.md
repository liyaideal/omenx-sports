# Project Memory

## Core
Large-catalog pickers (>12 options + search): mobile = full-screen Sheet (`TeamPickerSheet variant="sheet"`), desktop = centered Dialog (`variant="dialog"`). Never swap.

## Memories
- [Picker presentation](mem://design/picker-shell) — Sheet on mobile, Dialog on desktop; rule lives in DESIGN.md §8.
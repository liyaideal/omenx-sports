---
name: Activation dialogs
description: 3M reward pool / 10U voucher / deposit-match modals — Figma spec, hero image + Jersey 25 title, mobile bottom sheet
type: design
---
`ActivationDialog` (`src/components/sports/activation/`) implements the Figma dialogs (file `uJcLNbrCwofKGRfhKc6UgQ`). Layout, top to bottom: **hero image** (desktop 420×148, mobile 390×172; close × overlaid top-right, mobile also shows a 36×4 grabber) → **eyebrow pill** (`bg-white/5`, 12px uppercase, dot or Clock icon) → **Jersey 25 title** (desktop 42px, mobile 38px uppercase) with the accent word colored (desktop `#F5BA38` amber, mobile `text-neon`) → **subtitle** (Inter 14px `#9697A8`) → **CTA pill** 46px (desktop `bg-gradient-carnival` pink→purple→yellow, mobile `bg-gradient-neon`) in Jersey 25 24px. No `Maybe later` dismiss, no icon well, no highlight strip. Card = `bg-[#161023]` + `border-[#382b53]` desktop, plain `bg-[#161023]` mobile sheet. Desktop = centered `Dialog` 420 wide, mobile = bottom `Sheet` (`rounded-t-3xl`) per `mem://rules/mobile-bottom-sheet`. Hero PNGs live at `HERO_IMAGES` map in `ActivationDialog.tsx` — until populated, `VARIANT_PRESETS.placeholder*` renders a CSS gradient. Accent-word split defaults to first token; `deposit-match` overrides via `accentMatch: "Get 20U"`. Fonts: Jersey 25 loaded via Google Fonts `<link>` in `__root.tsx`, exposed as `font-jersey` utility. `--gradient-carnival` token lives in `src/styles.css`. Presets in `presets.tsx` unchanged. Playground: `/style-guide#activation`.